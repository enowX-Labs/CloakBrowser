# CloakBrowser API — Environment Variables
# Copy this file to .env and fill in your values
# cp .env.example .env

# ============================================================
# SECURITY
# ============================================================

# API key to protect your browser API from unauthorized access
# Leave empty to disable authentication (NOT recommended for production)
# Example: API_KEY=my-super-secret-key-change-this
API_KEY=

# Allowed CORS origins (comma-separated)
# Use * to allow all origins (default), or restrict to your domain
# Example: CORS_ORIGINS=https://myapp.com,https://api.myapp.com
CORS_ORIGINS=*


# ============================================================
# SESSION MANAGEMENT
# ============================================================

# Maximum number of concurrent browser sessions
# Each session uses ~200-500MB RAM, plan accordingly
MAX_SESSIONS=10

# Seconds before an idle session is automatically closed
# 3600 = 1 hour, 0 = never auto-close
SESSION_TTL=3600


# ============================================================
# SERVER
# ============================================================

# Port to listen on
PORT=7317

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO


# ============================================================
# RESOURCE LIMITS (docker-compose only)
# ============================================================

# Memory limit for the container
MEMORY_LIMIT=4g

# CPU limit (number of CPUs)
CPU_LIMIT=2.0


# ============================================================
# CLOAKBROWSER INTERNALS (advanced)
# ============================================================

# Override the Chromium binary cache directory
# CLOAKBROWSER_CACHE_DIR=/custom/path

# Use a locally built Chromium binary instead of downloading
# CLOAKBROWSER_BINARY_PATH=/path/to/chrome

# Override the download URL for the stealth Chromium binary
# CLOAKBROWSER_DOWNLOAD_URL=https://your-mirror.com
